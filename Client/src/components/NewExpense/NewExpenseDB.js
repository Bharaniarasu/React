import NewExpense from "./NewExpense";
import axios from "axios";

const NewExpenseDB = (props) => {
  console.log(props.db.date);
  console.log(props.db.amount);

  return <></>;
};

export default NewExpenseDB;
